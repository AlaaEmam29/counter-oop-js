"use strict"

import {Counter} from "./counter.js"

const firstCounter = new Counter(document.querySelector(".first-counter"),100)
const secondCounter = new Counter(document.querySelector(".second-counter"),200)